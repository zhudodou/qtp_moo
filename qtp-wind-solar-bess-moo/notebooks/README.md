# Notebooks

`666_algorithm_clean.ipynb` is a cleaned copy of the algorithm notebook with
cell outputs removed for GitHub upload. The notebook is kept for provenance and
interactive inspection.

For reproducible command-line execution, use:

```bash
python scripts/run_experiment.py --input data/MPDEV2018.nc --output results/MPDEV2018 --runs 10
```
