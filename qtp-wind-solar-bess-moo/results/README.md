This directory is reserved for generated optimization outputs.

The repository intentionally does not track result files such as NetCDF,
Excel, CSV, or figure outputs. Re-run `scripts/run_experiment.py` to regenerate
the outputs used for analysis.
